{
  "name": "driket",
  "private": true,
  "devDependencies": {
    "@types/node": "^11.9.4",
    "lerna": "^3.13.0",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "publish": "lerna run build && lerna publish",
    "start": "cd packages/react && yarn start",
    "develop": "cd packages/doc && yarn develop",
    "build": "lerna clean -y && lerna run build && lerna bootstrap"
  },
  "version": "0.0.9",
  "dependencies": {
    "@emotion/core": "^10.0.7",
    "@emotion/styled": "^10.0.7",
    "@types/react": "^16.8.4",
    "@types/react-dom": "^16.8.2",
    "react": "^16.8.2",
    "react-dom": "^16.8.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
