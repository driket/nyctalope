---
icon: 'layout'
---

import { Stack, Icon } from '@nyctalope/react';

# Stack

<DocGen componentMetadata={props.componentMetadata} />

## Horizontal direction

### Distribute 'start'

Items are packed toward the start line.

```jsx
<Stack distribute='start'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'end'

Items are packed toward to end line.

```jsx
<Stack distribute='end'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'center'

Items are centered along the line.

_Options_: no gap

```jsx
<Stack distribute='center'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space between'

Items are evenly distributed in the line; first item is on the start line, last item on the end line.

```jsx
<Stack distribute='space-between'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space around'

Items are evenly distributed in the line with equal space around them.

```jsx
<Stack distribute='space-around'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space evenly'

Items are distributed so that the spacing between any two adjacent alignment subjects, before the first alignment subject, and after the last alignment subject is the same.

```jsx
<Stack distribute='space-evenly'>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

## Vertical direction

### Distribute 'start' - vertical

```jsx
<Stack distribute='start' direction='vertical' style={{ minHeight: '150px' }}>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'end' - vertical

```jsx
<Stack distribute='end' direction='vertical' style={{ minHeight: '150px' }}>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'center' - vertical

```jsx
<Stack distribute='center' direction='vertical' style={{ minHeight: '150px' }}>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space between' - vertical

```jsx
<Stack
  distribute='space-between'
  direction='vertical'
  style={{ minHeight: '150px' }}
>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space around' - vertical

```jsx
<Stack
  distribute='space-around'
  direction='vertical'
  style={{ minHeight: '150px' }}
>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### Distribute 'space evenly' - vertical

```jsx
<Stack
  distribute='space-evenly'
  direction='vertical'
  style={{ minHeight: '150px' }}
>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

## Gap

When distribution is "start", "center" or "end", a gap can be passed as attribute.

### No gap

```jsx
<Stack distribute='start' gap={0}>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```

### 30px gap

```jsx
<Stack distribute='start' gap={30}>
  {Array(3)
    .fill('')
    .map((person) => (
      <Button icon='x-circle' />
    ))}
</Stack>
```
