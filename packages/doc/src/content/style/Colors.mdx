---
icon: 'file'
---

import { ColorDot, ColorPalette } from '../../components/ColorSample'
import { Wrapper, importer } from '../../../index'
const { Grid } = importer()

# Color Sample

<Wrapper
  bothColorSchemes
  colorSchemeSeparatorDirection='horizontal'
  checkerBoard
>
  <Grid numCol={3} numColMD={2} numColSM={1}>
    <ColorPalette themedColor={'main'} />
    <ColorPalette themedColor={'highlight'} />
    <ColorPalette themedColor={'background'} />
    <ColorPalette themedColor={'success'} />
    <ColorPalette themedColor={'danger'} />
    <ColorPalette themedColor={'grey'} />
    <ColorPalette themedColor={'lightGrey'} />
    <ColorPalette themedColor={'lighterGrey'} />
  </Grid>
</Wrapper>
