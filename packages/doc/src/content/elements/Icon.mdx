---
icon: 'layout'
---

import { Wrapper } from '../../components'
import { Icon, Stack, Grid } from '@nyctalope/react'

# Icons

Full list: https://feathericons.com

## Examples

<Wrapper>
  <Grid numCol={5} numColXS={1} numColSM={1} numColMD={2} numColLG={4}>
    {['file', 'circle', 'moon'].map((icon) => (
      <Stack direction='vertical'>
        <Icon icon={icon} />
        {icon}
      </Stack>
    ))}
    <span>...</span>
  </Grid>
</Wrapper>
