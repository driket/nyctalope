---
icon: 'layout'
---

import { Wrapper, PeoplePhoto } from '../../components'
import { Grid, Frame } from '@nyctalope/react'

# Grid

## Simple Grid

<Wrapper>
  <Grid numCol={5}>
    {Array(10)
      .fill('')
      .map((person) => (
        <PeoplePhoto />
      ))}
  </Grid>
</Wrapper>

## Responsive Grid

<Wrapper>
  <Grid numCol={5} numColXS={1} numColSM={1} numColMD={2} numColLG={4}>
    {Array(10)
      .fill('')
      .map((person) => (
        <PeoplePhoto />
      ))}
  </Grid>
</Wrapper>
